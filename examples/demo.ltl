// examples/demo.ltl — runnable demo for ltl_tictactoe

import std::io
use ltl_tictactoe as lib

fn main() {
    io::println("ltl_tictactoe demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}